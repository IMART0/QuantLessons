# Методические материалы: Урок 14 «Формы в Django»
**Дата проведения:** 2 июля  
**Время:** 10:00 - 11:20 (80 минут)  
**Аудитория:** Учащиеся ЛФМШ «Квант» (7-10 класс физико-математического профиля)

---

## 1. Паспорт урока

| Параметр | Описание |
| :--- | :--- |
| **Тема урока** | Формы в Django (Django Forms): создание, рендеринг, валидация и безопасность |
| **Продолжительность** | 80 минут (1 пара) |
| **Цель урока** | Изучить механизм работы с формами в Django, понять разницу между связанными и несвязанными формами, освоить декларативный синтаксис объявления полей и виджетов, научиться выполнять валидацию на уровне отдельных полей и всей формы, а также выводить формы в шаблонах с соблюдением требований безопасности (CSRF). |
| **Планируемые результаты обучения** | **Предметные:**<br>- Понимание роли форм в веб-приложении и архитектуры MVT в части обработки пользовательского ввода.<br>- Умение объявлять формы на базе класса `forms.Form`.<br>- Понимание разницы между валидацией полей (Field) и отрисовкой через виджеты (Widget).<br>- Умение реализовывать методы кастомной валидации `clean_<fieldname>()` и `clean()`.<br>- Владение навыками рендеринга форм в HTML-шаблонах (автоматический и ручной вывод).<br>- Понимание принципа работы CSRF-защиты.<br><br>**Метапредметные:**<br>- Развитие алгоритмического мышления при проектировании логики проверок.<br>- Формирование навыка декомпозиции задач (разделение интерфейса и бизнес-логики).<br><br>**Личностные:**<br>- Внимание к деталям и безопасности при разработке информационных систем. |
| **Входные требования (Prerequisites)** | - Базовый синтаксис Python (классы, наследование, переопределение методов, словари, исключения).<br>- Основы разметки HTML (теги `<form>`, `<input>`, `<label>`, `<select>`, `<button>`).<br>- Понятие HTTP-методов (GET и POST).<br>- Базовый опыт работы с Django (создание проекта/приложения, написание простых Views и HTML-шаблонов). |

---

## 2. Подробный поминутный план (80 минут)

*   **00:00 - 00:05 (5 минут) | Организационный момент.**
    Приветствие участников лагеря, проверка готовности рабочих мест (запуск VS Code, активация виртуального окружения, запуск сервера разработки). Анонс темы.
*   **00:05 - 00:15 (10 минут) | Проблематика и архитектура.**
    Обсуждение: почему ручной разбор `request.POST` — это долго и небезопасно. Введение в Django Forms как декларативный способ работы с вводом. Понятие связанной (Bound) и несвязанной (Unbound) формы.
*   **00:15 - 00:25 (10 минут) | Объявление форм и типы полей.**
    Синтаксис объявления полей формы в `forms.py`. Основные типы полей (`CharField`, `EmailField`, `IntegerField`, `ChoiceField` и др.). Понятие виджетов и отличие логики валидации от отрисовки.
*   **00:25 - 00:35 (10 минут) | Жизненный цикл формы во View.**
    Разбор стандартного паттерна обработки запроса: GET-запрос (отображение пустой формы) и POST-запрос (прием данных, валидация). Вызов `is_valid()` и получение данных через `cleaned_data`.
*   **00:35 - 00:45 (10 минут) | Рендеринг в шаблонах и безопасность.**
    Быстрый вывод (`as_p`, `as_table`). Необходимость `{% csrf_token %}` и объяснение уязвимости CSRF на пальцах. Ручной (построчный) вывод полей для кастомной верстки.
*   **00:45 - 01:00 (15 минут) | Кастомная валидация.**
    Разбор переопределения валидации. Метод `clean_<fieldname>()` для одного поля и метод `clean()` для совместной валидации нескольких полей (например, сравнение двух паролей). Использование встроенных валидаторов.
*   **01:00 - 01:15 (15 минут) | Лайв-кодинг демонстрация.**
    Совместное написание формы регистрации участника лагеря с проверкой возраста и паролей. Пошаговая отладка и демонстрация вывода ошибок.
*   **01:15 - 01:20 (5 минут) | Разбор домашнего задания и FAQs.**
    Объяснение требований к домашней работе, ответы на возникшие вопросы учащихся.

---

## 3. Структура презентации

Презентация состоит из **16 слайдов**, переключение которых осуществляется с помощью клавиатуры (стрелки, пробел) или интерактивных кнопок на экране.

| № Слайда | Название слайда | Основное содержание | Примечания для преподавателя | Время |
| :--- | :--- | :--- | :--- | :--- |
| 1 | Титульный | Логотип «Квант», тема «Формы в Django», дата, автор. | Настроить аудиторию, проверить готовность. | 2 мин |
| 2 | Зачем нужны веб-формы? | Проблемы ручного разбора POST-запросов, уязвимости, рутина валидации. | Привести пример с разбором сырого словаря. | 3 мин |
| 3 | Три ключевые задачи | Генерация HTML, валидация и очистка, вывод ошибок. | Показать концептуальное разделение обязанностей. | 3 мин |
| 4 | Объявление формы в коде | Создание `forms.py`, объявление полей, синтаксис класса. | Сравнить с объявлением моделей Django ORM. | 3 мин |
| 5 | Основные типы полей | Таблица: поле, HTML-элемент, тип в Python после очистки. | Обратить внимание на авто-преобразование типов. | 4 мин |
| 6 | Поля vs Виджеты | Разделение логики валидации и внешнего вида. Примеры. | Показать, как превратить CharField в Textarea или пароль. | 4 мин |
| 7 | Рендеринг в шаблоне | Использование `as_p()`, `as_table()`, важность `{% csrf_token %}`. | Акцентировать внимание на защите CSRF. | 3 мин |
| 8 | Ручная верстка полей | Полевой доступ: `field.label`, `field.errors`, `field.id_for_label`. | Показать, как интегрировать форму с Bootstrap/CSS. | 4 мин |
| 9 | Обработка формы во View | Стандартный паттерн с условием `if request.method == 'POST'`. | Разобрать прохождение запроса по двум веткам. | 5 мин |
| 10 | Как работает `is_valid()`? | Состояния Bound/Unbound, запуск валидации, словарь `errors`. | Подчеркнуть, что без вызова `is_valid` данные не очищаются. | 4 мин |
| 11 | Очищенные данные | Работа со словарем `cleaned_data`. Предостережения о KeyError. | Объяснить, почему нельзя обращаться к нему до `is_valid()`. | 3 мин |
| 12 | Валидация одного поля | Создание метода `clean_<fieldname>()`, обязательный возврат данных. | Предупредить о частой ошибке — забытом `return value`. | 4 мин |
| 13 | Валидация всей формы | Переопределение метода `clean()`, работа с несколькими полями. | Показать на примере сверки паролей. | 5 мин |
| 14 | Валидаторы (Validators) | Классы валидаторов из `django.core.validators`. Создание регулярных проверок. | Показать концепцию переиспользуемых проверок. | 4 мин |
| 15 | Типичные ошибки | Чек-лист частых багов начинающих разработчиков. | Заострить внимание на потере `return` и CSRF. | 4 мин |
| 16 | Резюме и практика | Подведение итогов, формулировка практической задачи. | Плавный переход к написанию кода. | 30 мин (вкл. практику) |

---

## 4. Инструкция по работе с презентацией

HTML-презентация спроектирована как адаптивное, автономное веб-приложение, оптимизированное для вывода на проектор.

*   **Местоположение файлов:** Презентация находится в папке `presentation/` в файле `index.html`. Дополнительно используются файлы стилей `styles.css` и логики `script.js`.
*   **Запуск:** Откройте файл `index.html` в любом современном веб-браузере (Google Chrome, Firefox, Yandex Browser, Microsoft Edge). Рекомендуется развернуть окно на весь экран.
*   **Управление слайдами:**
    *   **Вперед:** Нажмите клавишу **Стрелка вправо (Right Arrow)**, **Пробел (Space)** или кнопку **«Вперед»** на экране.
    *   **Назад:** Нажмите клавишу **Стрелка влево (Left Arrow)** или кнопку **«Назад»** на экране.
    *   **Полноэкранный режим:** Нажмите клавишу **F** на клавиатуре или кнопку **«Экран [F]»** в правом нижнем углу интерфейса.
*   **Особенности визуального оформления:**
    *   Светлая тема с высокой контрастностью текста (черно-синий на белом фоне) для хорошей читаемости даже на старых проекторах в солнечных аудиториях.
    *   Нижний индикатор прогресса (Progress Bar) наглядно показывает текущее положение в презентации.
    *   Интерактивные элементы: подсветка кода, выделенные блоки предупреждений (`warning`) и успехов (`success`).

---

## 5. Подробный сценарий преподавателя

### Слайд 1: Титульный
> **Слова преподавателя:** «Приветствую вас, квантовцы! Сегодня у нас очень важная тема, которая станет фундаментом для всех ваших интерактивных веб-приложений. Мы научимся принимать информацию от пользователей. До этого момента наши сайты умели только показывать данные из базы или статичный текст. Сегодня мы изучим Django Forms — инструмент, который спасет вас от написания сотен строк проверок и сделает ваши приложения безопасными. Запишите тему: "Формы в Django".»

### Слайд 2: Зачем нужны веб-формы?
> **Слова преподавателя:** «Вспомните любой сайт, которым вы пользуетесь ежедневно. Везде, где вам нужно войти, зарегистрироваться, написать комментарий, отфильтровать список товаров в интернет-магазине или загрузить файл — используются формы. 
> 
> Почему мы не можем просто прочитать данные из POST-запроса вручную? Представьте, что пользователь присылает нам свой возраст. Мы получаем строку `"17"`. Нам нужно: проверить, что строка состоит из цифр, преобразовать её в число `int`, проверить, что оно не отрицательное и не равно 200. А если это email? Нам придется писать сложные регулярные выражения для проверки формата `@`. Делать это вручную для десятка полей — кошмар программиста, порождающий кучу багов. Django предлагает красивое решение.»

### Слайд 3: Три ключевые задачи Django Forms
> **Слова преподавателя:** «В Django работа с формами строится вокруг класса `forms.Form`. Этот класс берет на себя три вещи. Во-первых, он сам генерирует HTML-код полей, избавляя вас от написания рутинной верстки. Во-вторых, он проверяет типы данных и очищает их — вырезает лишние пробелы, приводит строки к объектам чисел, дат и булевых значений. В-третьих, если пользователь ошибся, форма сама подскажет, в каком именно поле допущена ошибка и почему.»
> 
> **Вопрос к аудитории:** «Как вы думаете, почему приведение типов данных на сервере так важно? Почему нельзя просто проверить всё на клиенте с помощью JavaScript?»
> 
> **Ожидаемый ответ:** «JS на клиенте можно легко отключить или обойти, отправив запрос напрямую через утилиты вроде curl или Postman. Серверная валидация — это последний и самый надежный рубеж обороны.»

### Слайд 4: Объявление формы в коде
> **Слова преподавателя:** «Обычно для форм создают отдельный файл `forms.py` внутри папки приложения. Посмотрите на пример слайда. Мы наследуемся от класса `forms.Form`. Каждый атрибут класса — это поле нашей формы. Обратите внимание на сходство с моделями Django ORM. Мы указываем тип поля: `CharField`, `EmailField`, `IntegerField`. У каждого поля есть параметры: `label` (человекочитаемое название), `max_length` (ограничение длины), `required` (обязательно ли поле для заполнения, по умолчанию `True`).»

### Слайд 5: Основные типы полей
> **Слова преподавателя:** «Давайте посмотрим на соответствие полей формы HTML-разметке и типам данных в Python. `CharField` превратится в обычный `<input type="text">` и после проверки вернет нам стандартную строку. `EmailField` отрендерится как специальный почтовый input, а Django на сервере дополнительно проверит, действительно ли введенная строка похожа на почту. `ChoiceField` генерирует выпадающий список `<select>` — пользователь сможет выбрать только один вариант из предложенных нами.»

### Слайд 6: Поля vs Виджеты
> **Слова преподавателя:** «Это концептуальный момент, который часто путают новички. Запомните: **Поле (Field)** — это логика проверки данных. **Виджет (Widget)** — это внешний вид поля на экране.
> 
> Допустим, нам нужно поле для ввода пароля. С точки зрения логики — это обычная строка (`CharField`). Но на экране мы не должны показывать символы, вместо них должны быть точки. Для этого мы берем `CharField` и указываем ему виджет `forms.PasswordInput()`. Виджет меняет только HTML-представление, а валидацию по-прежнему выполняет поле.»

### Слайд 7: Рендеринг формы в шаблоне
> **Слова преподавателя:** «Как вывести форму на страницу? Мы передаем объект формы из View в шаблон. Самый простой способ — написать `{{ form.as_p }}` внутри тега `<form>`. Django автоматически обернет каждое поле вместе с его label и текущими ошибками в тег абзаца `<p>`.
> 
> Но обратите внимание на обязательную деталь: тег `{% csrf_token %}`. Без него отправка формы методом POST вызовет ошибку 403 Forbidden. Это защита от межсайтовой подделки запросов. Токен гарантирует, что форма была отправлена именно с нашего сайта, а не вредоносным скриптом с постороннего ресурса.»

### Слайд 8: Тонкая настройка рендеринга
> **Слова преподавателя:** «Автоматический рендеринг `as_p` хорош для черновиков, но в реальных проектах дизайн требует кастомной верстки. Django позволяет выводить элементы формы по отдельности. Мы пишем `{{ form.email }}` для вывода самого input-поля, `{{ form.email.label }}` — для вывода текстовой метки, и `{{ form.email.errors }}` — для отображения списка ошибок этого поля. Это позволяет легко стилизовать форму с помощью CSS или фреймворков вроде Bootstrap.»

### Слайд 9: Жизненный цикл формы во View
> **Слова преподавателя:** «Теперь разберем, как написать View, обрабатывающий форму. Здесь используется классический паттерн. 
> 
> Если к нам пришел GET-запрос (пользователь впервые открыл страницу), мы создаем пустой объект формы: `form = ContactForm()`. Такая форма называется *несвязанной* (unbound).
> 
> Если пришел POST-запрос (пользователь нажал кнопку отправки), мы создаем форму и передаем ей данные из запроса: `form = ContactForm(request.POST)`. Такая форма называется *связанной* (bound). Далее мы вызываем `form.is_valid()`. Если проверка успешна, мы работаем с данными, иначе — возвращаем ту же форму (уже наполненную введенными данными и ошибками) обратно пользователю.»

### Слайд 10: Как работает `is_valid()`?
> **Слова преподавателя:** «Что происходит внутри метода `is_valid()`? Django запускает цепочку проверок. Сначала каждое поле проверяет само себя (например, `IntegerField` проверяет, прислали ли ему число). Если все поля прошли базовую проверку, запускаются кастомные методы очистки, о которых мы поговорим далее. Если ошибок нет, метод возвращает `True`. Если хотя бы одно поле содержит ошибку, `is_valid()` возвращает `False`, а все сообщения об ошибках складываются в словарь `form.errors`.»

### Слайд 11: Работа со словарём `cleaned_data`
> **Слова преподавателя:** «После успешной валидации все очищенные и приведенные к Python-типам данные сохраняются в словаре `form.cleaned_data`. Если поле `age` было строкой `"15"`, в `cleaned_data['age']` вы получите чистое целое число `15`. 
> 
> **Внимание!** До вызова `is_valid()` обращаться к `cleaned_data` нельзя — вы получите ошибку `AttributeError`. Также помните: если поле не прошло валидацию, его не будет в этом словаре. Используйте метод `.get()`, чтобы избежать падения программы.»

### Слайд 12: Валидация одного поля: `clean_<fieldname>()`
> **Слова преподавателя:** «Иногда встроенных проверок полей не хватает. Например, мы хотим запретить регистрировать пользователей с именами, содержащими слово "admin". Для этого мы можем переопределить метод `clean_<имя_поля>()` прямо в классе формы.
> 
> Внутри метода мы берем значение из `self.cleaned_data`, выполняем свою проверку и, если что-то не так, возбуждаем исключение `forms.ValidationError`. 
> 
> **Критически важное правило:** метод `clean_<fieldname>()` должен **обязательно** возвращать очищенное значение в конце! Если вы забудете написать `return username`, поле вернет `None`, и вы потеряете данные.»

### Слайд 13: Валидация формы: метод `clean()`
> **Слова преподавателя:** «А что делать, если нужно проверить зависимость полей друг от друга? Например, проверить, что введенный пароль и его подтверждение совпадают. Метод одного поля здесь не поможет, потому что он не знает про другие поля.
> 
> Для этого мы переопределяем общий метод `clean()`. Сначала мы вызываем `super().clean()`, чтобы получить данные от всех полей. Затем достаем нужные поля и сравниваем их. Если пароли различаются, мы генерируем `ValidationError`. Этот метод должен вернуть очищенный словарь `cleaned_data` в конце.»

### Слайд 14: Кастомные валидаторы (Validators)
> **Слова преподавателя:** «Иногда логику проверки хочется переиспользовать в разных формах. Для этого в Django есть механизм валидаторов. Валидатор — это функция, которая проверяет значение и бросает `ValidationError` при ошибке. Мы можем передавать список таких функций в параметр `validators` любого поля формы. В Django также есть полезные встроенные валидаторы, такие как `RegexValidator` для проверки строк по регулярным выражениям.»

### Слайд 15: Типичные ошибки
> **Слова преподавателя:** «Давайте зафиксируем главные ошибки, на которые наступают все новички:
> 1. Забывают вернуть очищенное значение из метода `clean_<fieldname>()`.
> 2. Забывают вставить `{% csrf_token %}` в HTML-шаблон.
> 3. Пытаются прочитать `cleaned_data` до вызова `is_valid()`.
> 4. Пытаются достать из `cleaned_data` поле, которое завалило валидацию, без использования метода `.get()`.
> Запомните этот слайд, он сбережет вам кучу времени при отладке.»

### Слайд 16: Резюме и практическое задание
> **Слова преподавателя:** «Итак, мы разобрали всю теорию работы с формами. Теперь давайте перейдем к практике. Наша задача — написать форму регистрации для участия в нашей школе "Квант". Переходим к лайв-кодингу!»

---

## 6. Демонстрации (Live Coding)

В рамках демонстрации преподаватель создает простое приложение с формой регистрации участника лагеря.

### Структура учебного проекта Django
```text
kvant_project/
│
├── kvant_project/
│   ├── __init__.py
│   ├── settings.py
│   ├── urls.py
│   └── wsgi.py
│
├── camp/
│   ├── __init__.py
│   ├── admin.py
│   ├── apps.py
│   ├── forms.py         <-- Файл с описанием наших форм
│   ├── migrations/
│   ├── models.py
│   ├── templates/
│   │   └── camp/
│   │       ├── register.html  <-- Шаблон формы регистрации
│   │       └── success.html   <-- Шаблон успешной отправки
│   ├── tests.py
│   └── views.py         <-- Контроллер обработки формы
│
└── manage.py
```

### Код: `camp/forms.py`
```python
from django import forms
from django.core.validators import MinValueValidator, MaxValueValidator
import datetime

class CampRegistrationForm(forms.Form):
    # Поля формы
    full_name = forms.CharField(
        label="ФИО участника",
        max_length=150,
        widget=forms.TextInput(attrs={
            'class': 'form-control',
            'placeholder': 'Иванов Иван Иванович'
        })
    )
    
    email = forms.EmailField(
        label="Электронная почта для связи",
        widget=forms.EmailInput(attrs={
            'class': 'form-control',
            'placeholder': 'example@kvant.org'
        })
    )
    
    birth_year = forms.IntegerField(
        label="Год рождения",
        validators=[
            MinValueValidator(1900, message="Год рождения не может быть таким маленьким!"),
            MaxValueValidator(datetime.date.today().year, message="Вы еще не родились!")
        ],
        widget=forms.NumberInput(attrs={
            'class': 'form-control',
            'placeholder': 'Например, 2010'
        })
    )
    
    direction = forms.ChoiceField(
        label="Направление обучения",
        choices=[
            ('phys', 'Физика'),
            ('math', 'Математика'),
            ('inf', 'Информатика'),
        ],
        widget=forms.Select(attrs={'class': 'form-select'})
    )
    
    password = forms.CharField(
        label="Пароль",
        widget=forms.PasswordInput(attrs={
            'class': 'form-control',
            'placeholder': 'Придумайте надежный пароль'
        })
    )
    
    password_confirm = forms.CharField(
        label="Подтверждение пароля",
        widget=forms.PasswordInput(attrs={
            'class': 'form-control',
            'placeholder': 'Повторите пароль'
        })
    )
    
    agreement = forms.BooleanField(
        label="Согласие на обработку персональных данных",
        required=True,
        widget=forms.CheckboxInput(attrs={'class': 'form-check-input'})
    )

    # Валидация отдельного поля: проверяем год рождения (участники лагеря должны быть от 12 до 17 лет)
    def clean_birth_year(self):
        birth_year = self.cleaned_data.get('birth_year')
        current_year = datetime.date.today().year
        age = current_year - birth_year
        
        if age < 12 or age > 17:
            raise forms.ValidationError(
                f"К сожалению, ваш возраст ({age} лет) не подходит. Лагерь принимает участников от 12 до 17 лет."
            )
        
        # Важно вернуть значение!
        return birth_year

    # Валидация всей формы: сверяем пароли
    def clean(self):
        cleaned_data = super().clean()
        password = cleaned_data.get('password')
        password_confirm = cleaned_data.get('password_confirm')
        
        if password and password_confirm and password != password_confirm:
            # Связываем ошибку конкретно с полем password_confirm
            self.add_error('password_confirm', "Введенные пароли не совпадают!")
            
        return cleaned_data
```

#### Разбор кода `camp/forms.py` построчно:
*   `from django import forms` — Импортируем модуль форм Django.
*   `class CampRegistrationForm(forms.Form)` — Создаем класс формы, наследуясь от базового класса `Form`.
*   `full_name = forms.CharField(...)` — Объявляем строковое поле. Через параметр `widget` мы переопределяем HTML-представление поля, передавая ему CSS-класс `form-control` (стандартный класс Bootstrap) и `placeholder`.
*   `birth_year = forms.IntegerField(...)` — Числовое поле для ввода года рождения. С помощью параметра `validators` задаем ограничения: год должен быть больше 1900 и не больше текущего года.
*   `direction = forms.ChoiceField(...)` — Поле выбора с кортежем вариантов `choices`. На первом месте в кортеже — техническое значение, отправляемое на сервер (например, `'phys'`), на втором — отображаемый текст (например, `'Физика'`).
*   `password = forms.CharField(..., widget=forms.PasswordInput)` — Строковое поле, символы которого при вводе маскируются точками благодаря виджету `PasswordInput`.
*   `agreement = forms.BooleanField(..., required=True)` — Чекбокс. Флаг `required=True` обязывает пользователя отметить эту галочку для успешной отправки формы.
*   `def clean_birth_year(self):` — Метод кастомной валидации поля `birth_year`. Вычисляет примерный возраст. Если он выходит за рамки [12, 17], генерирует ошибку. В конце обязательно возвращает проверенный `birth_year`.
*   `def clean(self):` — Метод совместной валидации. Вызывает родительский `super().clean()`, забирает пароли и сравнивает их. Метод `self.add_error()` вешает ошибку на конкретное поле `password_confirm`, чтобы она отрисовалась именно под ним в интерфейсе.

---

### Код: `camp/views.py`
```python
from django.shortcuts import render, redirect
from .forms import CampRegistrationForm

def register_view(request):
    if request.method == 'POST':
        # Если пришел POST-запрос, связываем форму с данными из request.POST
        form = CampRegistrationForm(request.POST)
        
        # Запуск валидации
        if form.is_valid():
            # Если форма валидна, данные очищены и лежат в cleaned_data
            full_name = form.cleaned_data['full_name']
            email = form.cleaned_data['email']
            direction = form.cleaned_data['direction']
            
            # Имитируем сохранение или бизнес-логику
            print(f"Успешная регистрация: {full_name} ({email}) на направление {direction}")
            
            # Перенаправляем на страницу успешной регистрации
            return render(request, 'camp/success.html', {'full_name': full_name})
    else:
        # Если GET-запрос, отдаем пустую форму
        form = CampRegistrationForm()
        
    return render(request, 'camp/register.html', {'form': form})
```

#### Разбор кода `camp/views.py` построчно:
*   `if request.method == 'POST':` — Разделяем логику. Форма отправляется методом POST, поэтому при первом посещении сработает ветка `else` (GET-запрос).
*   `form = CampRegistrationForm(request.POST)` — Передаем словарь POST-данных конструктору формы. Теперь форма «связана» с данными (Bound Form).
*   `if form.is_valid():` — Запускаем валидацию. Этот метод автоматически вызывает базовые валидаторы полей, затем кастомный метод `clean_birth_year()`, а затем метод `clean()`.
*   `full_name = form.cleaned_data['full_name']` — Извлекаем гарантированно корректные и очищенные данные.
*   `return render(request, 'camp/success.html', ...)` — Рендерим шаблон успеха при успешном прохождении проверок.
*   `form = CampRegistrationForm()` — В ветке `else` создаем пустую «несвязанную» форму (Unbound Form).
*   `return render(request, 'camp/register.html', {'form': form})` — Возвращаем страницу с формой. Обратите внимание: если валидация POST-запроса не прошла, выполнение перейдет к этой строке, и на страницу вернется та же форма `form` с уже введенными данными и сгенерированными ошибками внутри.

---

### Код: `camp/templates/camp/register.html`
```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Регистрация в ЛФМШ "Квант"</title>
  <!-- Подключаем Bootstrap для красоты -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background-color: #f4f7f6; padding-top: 50px; }
    .form-container { background: white; padding: 30px; border-radius: 10px; box-shadow: 0 4px 15px rgba(0,0,0,0.1); }
    .errorlist { list-style-type: none; color: #dc3545; padding-left: 0; font-size: 0.875rem; margin-top: 5px; }
  </style>
</head>
<body>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-6 form-container">
      <h2 class="text-center mb-4 text-primary">Регистрация в "Квант"</h2>
      
      <form method="post" novalidate>
        <!-- novalidate отключает стандартную валидацию браузера, чтобы мы увидели работу Django -->
        {% csrf_token %}
        
        <!-- Выводим общие ошибки формы (не привязанные к полям) -->
        {% if form.non_field_errors %}
          <div class="alert alert-danger">
            {{ form.non_field_errors }}
          </div>
        {% endif %}
        
        <!-- Ручной вывод каждого поля для контроля верстки -->
        <div class="mb-3">
          <label class="form-label">{{ form.full_name.label }}</label>
          {{ form.full_name }}
          {% if form.full_name.errors %}
            <div class="text-danger small">{{ form.full_name.errors }}</div>
          {% endif %}
        </div>

        <div class="mb-3">
          <label class="form-label">{{ form.email.label }}</label>
          {{ form.email }}
          {% if form.email.errors %}
            <div class="text-danger small">{{ form.email.errors }}</div>
          {% endif %}
        </div>

        <div class="mb-3">
          <label class="form-label">{{ form.birth_year.label }}</label>
          {{ form.birth_year }}
          {% if form.birth_year.errors %}
            <div class="text-danger small">{{ form.birth_year.errors }}</div>
          {% endif %}
        </div>

        <div class="mb-3">
          <label class="form-label">{{ form.direction.label }}</label>
          {{ form.direction }}
          {% if form.direction.errors %}
            <div class="text-danger small">{{ form.direction.errors }}</div>
          {% endif %}
        </div>

        <div class="mb-3">
          <label class="form-label">{{ form.password.label }}</label>
          {{ form.password }}
          {% if form.password.errors %}
            <div class="text-danger small">{{ form.password.errors }}</div>
          {% endif %}
        </div>

        <div class="mb-3">
          <label class="form-label">{{ form.password_confirm.label }}</label>
          {{ form.password_confirm }}
          {% if form.password_confirm.errors %}
            <div class="text-danger small">{{ form.password_confirm.errors }}</div>
          {% endif %}
        </div>

        <div class="mb-3 form-check">
          {{ form.agreement }}
          <label class="form-check-label">{{ form.agreement.label }}</label>
          {% if form.agreement.errors %}
            <div class="text-danger small">{{ form.agreement.errors }}</div>
          {% endif %}
        </div>

        <button type="submit" class="btn btn-primary w-100">Зарегистрироваться</button>
      </form>
    </div>
  </div>
</div>

</body>
</html>
```

#### Разбор HTML-шаблона:
*   `novalidate` — Атрибут формы, отключающий встроенную валидацию HTML5 браузера. Это нужно на этапе разработки, чтобы убедиться, что бэкенд на Django корректно отлавливает ошибки и выводит их.
*   `{% csrf_token %}` — Вставляет скрытый input со случайным токеном безопасности. Django сверяет его при получении POST-запроса.
*   `{{ form.non_field_errors }}` — Ошибки, возбужденные в методе `clean()` и не привязанные к конкретным полям (глобальные ошибки формы).
*   `{{ form.full_name }}` — Выводит сам HTML-тег `<input>` с атрибутами, которые мы прописали в `forms.py` (класс, placeholder).
*   `{{ form.full_name.errors }}` — Если при обработке формы в поле ФИО возникла ошибка, Django выведет её в виде тега списка ошибок. Мы оборачиваем вывод в контейнер с классом `text-danger` для стилизации.

---

### Код: `camp/templates/camp/success.html`
```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Регистрация успешна</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container text-center mt-5">
  <div class="card p-5 shadow-sm mx-auto" style="max-width: 500px;">
    <h1 class="text-success mb-4">Успешно!</h1>
    <p class="lead">Участник <strong>{{ full_name }}</strong> успешно зарегистрирован на смену ЛФМШ "Квант".</p>
    <a href="{% url 'register' %}" class="btn btn-outline-primary mt-3">Вернуться к форме</a>
  </div>
</div>

</body>
</html>
```

---

## 7. Практические задачи (Exercises)

Ниже приведены 6 практических задач разного уровня сложности для отработки материала на уроке.

### Задача 1 (Базовая): Форма обратной связи (FeedbackForm)
**Условие:** Создать форму обратной связи `FeedbackForm` со следующими полями:
1. `name` (Имя) — текстовое поле, максимальная длина 50 символов.
2. `email` (Электронная почта) — валидный email-адрес.
3. `rating` (Оценка) — выбор из вариантов 1, 2, 3, 4, 5. Оформить в виде выпадающего списка.
4. `feedback` (Отзыв) — текстовое многострочное поле.

**Подсказка:** Для поля `feedback` используйте `CharField` с переопределением виджета на `forms.Textarea`. Варианты выбора передаются через кортеж кортежей.

**Решение:**
```python
from django import forms

class FeedbackForm(forms.Form):
    RATING_CHOICES = [
        (1, 'Ужасно'),
        (2, 'Плохо'),
        (3, 'Нормально'),
        (4, 'Хорошо'),
        (5, 'Отлично'),
    ]
    
    name = forms.CharField(label="Ваше имя", max_length=50)
    email = forms.EmailField(label="Электронная почта")
    rating = forms.ChoiceField(label="Оценка лагеря", choices=RATING_CHOICES)
    feedback = forms.CharField(label="Отзыв", widget=forms.Textarea)
```
**Разбор кода:** Мы объявили список вариантов `RATING_CHOICES`. Обратите внимание, что значения ключей целые числа (однако Django Forms при отправке в запросе передает их как строки, поэтому при работе с ChoiceField после `is_valid` тип значения приводится к строке, если типы ключей не были обработаны кастомно. Для строгого приведения типов можно использовать `TypedChoiceField(coerce=int)`).

---

### Задача 2 (Базовая): Кастомная валидация возраста (clean_age)
**Условие:** В форму `UserForm` с числовым полем `age` добавить кастомную валидацию, которая проверяет, что возраст пользователя не меньше 18 лет и не больше 120 лет. Если возраст выходит за эти рамки, возбуждать `ValidationError` с текстом: «Возраст должен быть от 18 до 120 лет!».

**Подсказка:** Напишите метод `clean_age(self)`. Данные извлекайте из словаря `self.cleaned_data`. Не забудьте вернуть проверенное значение.

**Решение:**
```python
from django import forms

class UserForm(forms.Form):
    name = forms.CharField(label="Имя")
    age = forms.IntegerField(label="Возраст")
    
    def clean_age(self):
        age = self.cleaned_data.get('age')
        if age < 18 or age > 120:
            raise forms.ValidationError("Возраст должен быть от 18 до 120 лет!")
        return age
```
**Разбор кода:** Метод `clean_age` автоматически вызывается при вызове `is_valid()`. Мы безопасно получаем `age`. Если условие `age < 18 or age > 120` выполняется, бросается исключение `ValidationError`. Оно перехватывается фреймворком и автоматически привязывается к полю `age`, которое затем выведет ошибку в шаблоне.

---

### Задача 3 (Базовая): Форма поиска с начальным значением
**Условие:** Создать форму поиска `SearchForm` с полем `query` (строка, необязательное) и `category` (выбор из категорий: "Книги", "Фильмы", "Игры"). Поле `category` по умолчанию должно иметь значение "Книги" при выводе. Поле `query` должно содержать HTML-атрибут `placeholder="Введите поисковый запрос..."`.

**Подсказка:** Для необязательного поля используйте параметр `required=False`. Начальное значение задается через `initial`. Атрибуты виджета передаются в словаре `attrs`.

**Решение:**
```python
from django import forms

class SearchForm(forms.Form):
    CATEGORY_CHOICES = [
        ('books', 'Книги'),
        ('movies', 'Фильмы'),
        ('games', 'Игры'),
    ]
    
    query = forms.CharField(
        label="Поиск",
        required=False,
        widget=forms.TextInput(attrs={'placeholder': 'Введите поисковый запрос...'})
    )
    category = forms.ChoiceField(
        label="Категория",
        choices=CATEGORY_CHOICES,
        initial='books'
    )
```
**Разбор кода:** Параметр `required=False` отключает обязательность ввода для поля `query`, позволяя отправлять пустые поисковые запросы. Параметр `initial='books'` указывает, что при первом рендере формы (GET-запрос) в теге `<select>` будет автоматически выбран `<option>` со значением `books`.

---

### Задача 4 (Средняя): Валидация домена почты (clean_email)
**Условие:** В форме регистрации `CampRegisterForm` необходимо разрешить регистрацию только пользователям с почтой в доменах `kvant.org` или `yandex.ru`. Любой другой домен должен вызывать ошибку валидации поля `email`: "Регистрация доступна только для доменов kvant.org и yandex.ru".

**Подсказка:** В методе `clean_email()` разделите строку почты по символу `@` на две части с помощью метода `split()`. Проверьте вторую часть (домен) на вхождение в список разрешенных.

**Решение:**
```python
from django import forms

class CampRegisterForm(forms.Form):
    full_name = forms.CharField(label="ФИО")
    email = forms.EmailField(label="Email")
    
    def clean_email(self):
        email = self.cleaned_data.get('email')
        
        # email гарантированно валиден по формату на этом этапе благодаря EmailField
        if '@' in email:
            domain = email.split('@')[-1].lower()
            allowed_domains = ['kvant.org', 'yandex.ru']
            if domain not in allowed_domains:
                raise forms.ValidationError(
                    "Регистрация доступна только для доменов kvant.org и yandex.ru"
                )
        
        return email
```
**Разбор кода:** Мы безопасно достаем `email`. Разделяем его по `@` и берем последний элемент списка (домен). Переводим его в нижний регистр через `.lower()`, чтобы сравнение было нечувствительно к регистру (например, `YANDEX.RU` и `yandex.ru` должны обрабатываться одинаково). Если домен отсутствует в списке разрешенных, бросается ошибка.

---

### Задача 5 (Средняя): Сравнение двух полей пароля (clean)
**Условие:** Создать форму смены пароля `PasswordChangeForm`, которая содержит три поля: `old_password` (старый пароль), `new_password` (новый пароль) и `confirm_new_password` (подтверждение нового пароля). Все поля должны использовать виджет скрытия символов. Напишите валидацию, которая гарантирует:
1. Новый пароль не совпадает со старым паролем. В случае совпадения выводить ошибку: "Новый пароль не должен совпадать со старым!".
2. Новый пароль совпадает с подтверждением нового пароля. При несовпадении выводить ошибку: "Новые пароли не совпадают!".

**Подсказка:** Переопределите метод `clean()`. Используйте `self.add_error(field_name, error_message)` для привязки ошибки к конкретному полю.

**Решение:**
```python
from django import forms

class PasswordChangeForm(forms.Form):
    old_password = forms.CharField(
        label="Старый пароль",
        widget=forms.PasswordInput()
    )
    new_password = forms.CharField(
        label="Новый пароль",
        widget=forms.PasswordInput()
    )
    confirm_new_password = forms.CharField(
        label="Подтвердите новый пароль",
        widget=forms.PasswordInput()
    )
    
    def clean(self):
        cleaned_data = super().clean()
        old_pass = cleaned_data.get('old_password')
        new_pass = cleaned_data.get('new_password')
        confirm_pass = cleaned_data.get('confirm_new_password')
        
        # Проверяем, что все поля успешно заполнили базовую валидацию
        if old_pass and new_pass and confirm_pass:
            if old_pass == new_pass:
                self.add_error('new_password', "Новый пароль не должен совпадать со старым!")
            
            if new_pass != confirm_pass:
                self.add_error('confirm_new_password', "Новые пароли не совпадают!")
                
        return cleaned_data
```
**Разбор кода:** Мы используем `self.add_error()` вместо классического `raise ValidationError`. Преимущество этого подхода в том, что мы можем повесить конкретные ошибки на конкретные поля (например, на `new_password` и `confirm_new_password`), а не генерировать одну общую неструктурированную ошибку для всей формы. Ошибки отразятся прямо рядом с нужными полями в шаблоне.

---

### Задача 6 (Сложная): Форма бронирования с проверкой перекрытия дат
**Условие:** Напишите форму бронирования комнаты в лагере `CampBookingForm`. 
Поля:
1. `room_number` (IntegerField) — номер комнаты.
2. `check_in` (DateField) — дата заезда (использовать виджет выбора даты `type='date'`).
3. `check_out` (DateField) — дата выезда (виджет выбора даты `type='date'`).

Напишите полную валидацию формы в методе `clean()`:
1. Дата заезда (`check_in`) должна быть не раньше сегодняшней даты. Для тестирования используйте фиксированную дату `2026-07-02` (дата нашего урока). Если заезд раньше, выдать ошибку полю `check_in`: "Дата заезда не может быть в прошлом!".
2. Дата выезда (`check_out`) должна быть строго позже даты заезда. При несоблюдении выдать ошибку полю `check_out`: "Дата выезда должна быть позже даты заезда!".
3. Разница между датой заезда и выезда (длительность смены) должна быть не более 14 дней. В противном случае вешать ошибку на форму в целом (non-field error) с текстом: "Максимальный срок бронирования — 14 дней!".

**Подсказка:** Для работы с датами используйте `datetime.date`. Разница между объектами дат возвращает объект `datetime.timedelta`.

**Решение:**
```python
from django import forms
import datetime

class CampBookingForm(forms.Form):
    room_number = forms.IntegerField(
        label="Номер комнаты",
        widget=forms.NumberInput(attrs={'class': 'form-control'})
    )
    check_in = forms.DateField(
        label="Дата заезда",
        widget=forms.DateInput(attrs={'type': 'date', 'class': 'form-control'})
    )
    check_out = forms.DateField(
        label="Дата выезда",
        widget=forms.DateInput(attrs={'type': 'date', 'class': 'form-control'})
    )
    
    def clean(self):
        cleaned_data = super().clean()
        check_in = cleaned_data.get('check_in')
        check_out = cleaned_data.get('check_out')
        
        # Фиксированная сегодняшняя дата по условию задачи (2 июля 2026 года)
        today = datetime.date(2026, 7, 2)
        
        # Проверяем, что даты введены и базово валидны
        if check_in and check_out:
            # 1. Проверка даты заезда
            if check_in < today:
                self.add_error('check_in', "Дата заезда не может быть в прошлом!")
                
            # 2. Проверка даты выезда относительно даты заезда
            if check_out <= check_in:
                self.add_error('check_out', "Дата выезда должна быть позже даты заезда!")
            
            # 3. Проверка длительности проживания (если первые две проверки не провалены)
            if check_in >= today and check_out > check_in:
                duration = check_out - check_in
                if duration.days > 14:
                    # Выбрасываем общую ошибку формы
                    raise forms.ValidationError("Максимальный срок бронирования — 14 дней!")
                    
        return cleaned_data
```
**Разбор кода:** Мы извлекаем даты и сравниваем их. Обратите внимание, что мы проверяем существование `check_in` и `check_out` перед сравнением, чтобы избежать ошибки `TypeError: '<' not supported between 'NoneType' and 'datetime.date'` (если пользователь не ввел даты вовсе). Для вычисления разницы мы вычитаем одну дату из другой: `duration = check_out - check_in`, получая объект `timedelta`, у которого свойство `.days` возвращает количество дней в виде целого числа. При превышении 14 дней мы используем `raise forms.ValidationError(...)` — эта ошибка не привязывается к конкретному полю и попадет в `form.non_field_errors`.

---

## 8. Домашнее задание (Homework)

Домашнее задание состоит из двух частей: обязательной и дополнительной («звездной»).

### Обязательное задание: Форма добавления статьи в блог (PostForm)
Создайте форму добавления статьи `PostForm` со следующими полями:
1. `title` (Заголовок) — `CharField` с ограничением длины в 100 символов.
2. `content` (Текст статьи) — текстовое поле с виджетом `Textarea`.
3. `status` (Статус статьи) — `ChoiceField` с вариантами выбора: `('draft', 'Черновик')`, `('pub', 'Опубликовано')`.
4. `published_date` (Дата публикации) — `DateField` с виджетом выбора даты (`type='date'`). Поле не должно быть обязательным для заполнения (`required=False`).

**Добавьте валидацию:**
*   В метод `clean_title()`: заголовок должен быть длиной не менее 5 символов и не должен начинаться со спецсимволов или знаков препинания (допустимы только буквы и цифры в качестве первого символа).
*   В метод `clean()`: если статус статьи выбран как `"Опубликовано"`, то поле `published_date` обязательно должно быть заполнено и не должно быть датой в будущем (относительно текущей даты). Если статус — `"Черновик"`, то дата публикации может оставаться пустой.

---

### Дополнительное задание (со звездочкой ⭐): Форма участника олимпиады
Создайте форму регистрации на олимпиаду `OlympiadRegisterForm` со следующими полями:
*   `fio` (ФИО участника) — строка.
*   `school_class` (Класс обучения) — `ChoiceField` с выбором классов от 7 до 11.
*   `subject` (Предмет олимпиады) — `ChoiceField` с вариантами: `('math', 'Математика')`, `('phys', 'Физика')`, `('inf', 'Информатика')`.

**Реализуйте сложную взаимную валидацию:**
1. Олимпиада по *Информатике* проводится только для учеников **не младше 9 класса**. При попытке зарегистрировать ученика 7 или 8 класса на информатику, должна выводиться ошибка у поля `subject`: "Олимпиада по информатике доступна только для учеников 9-11 классов!".
2. Олимпиада по *Физике* проводится только **с 8 класса** (в 7 классе физику еще не изучают системно). При попытке зарегистрировать ученика 7 класса на физику, должна выводиться ошибка у поля `subject`: "Олимпиада по физике доступна только начиная с 8 класса!".

---

### Критерии оценивания домашнего задания:
*   **10 баллов:** Все поля объявлены корректно с соответствующими типами и виджетами. Все требования к валидации (как для отдельных полей, так и сквозные в методе `clean()`) выполнены в полном объеме. Корректно обработана вероятность отсутствия значений (`None`) в `cleaned_data`.
*   **8 баллов:** Задание работает, но допущены мелкие неточности (например, валидация полей падает с ошибкой `AttributeError`, если какое-то из полей не было заполнено пользователем, из-за некорректного использования `.get()` в `cleaned_data`).
*   **6 баллов:** Форма создана, но валидация реализована частично (например, реализована проверка заголовка, но не работает взаимная проверка статуса и даты).
*   **4 балла и менее:** Код содержит критические синтаксические ошибки или структура формы полностью нарушена.

---

## 9. Часто задаваемые вопросы (FAQs)

### 1. В чем разница между `forms.Form` и `forms.ModelForm`?
`forms.Form` — это базовая форма общего назначения. Мы вручную описываем каждое поле и пишем логику валидации. `forms.ModelForm` — специализированный класс, который автоматически генерирует поля на основе существующей модели базы данных (Django Model) и умеет автоматически сохранять данные в БД. На текущем уроке мы изучаем базовые формы `forms.Form`, чтобы понять фундаментальный жизненный цикл форм, валидацию и очистку данных.

### 2. Зачем нужен метод `super().clean()` в переопределенном методе `clean()`?
Метод `super().clean()` запускает базовую валидацию всех полей формы. Он собирает корректно заполненные поля в словарь `self.cleaned_data`. Если вы не вызовете `super().clean()`, этот словарь может не инициализироваться корректно, или в нем будут отсутствовать результаты базовых проверок полей.

### 3. Почему при ошибках в полях метод `clean()` падает с ошибкой `AttributeError` при вызове `cleaned_data.get()`?
Если поля формы не прошли базовую валидацию (например, вместо числа ввели буквы в `IntegerField`), они вообще не попадут в `cleaned_data`. Если вы попытаетесь обратиться к ним напрямую: `cleaned_data['my_field']`, Python выдаст ошибку `KeyError`. Поэтому в методе `clean()` критически важно использовать безопасный метод словарей `.get()`: `cleaned_data.get('my_field')`, который вернет `None`, если поле не прошло валидацию.

### 4. Что делает параметр `novalidate` в HTML-теге `<form>`?
Он указывает браузеру отключить встроенную HTML5-валидацию полей (например, автоматическую проверку формата email-адреса силами браузера Chrome/Firefox). Это очень полезно при разработке бэкенда, чтобы проверить, как бэкенд на Django обрабатывает некорректный ввод и отображает кастомные ошибки.

### 5. Можно ли стилизовать автосгенерированную через `{{ form.as_p }}` форму с помощью CSS?
Да, можно, но это неудобно. Django рендерит поля со стандартными именами и ID (например, `id="id_username"`). Вы можете писать CSS-селекторы под эти ID. Однако для профессиональной верстки (например, интеграции сетки Bootstrap или Tailwind CSS) используют либо ручной рендеринг полей в шаблоне, либо специальные библиотеки вроде `django-crispy-forms`.

### 6. Почему Django выдает ошибку "Forbidden (403) CSRF verification failed. Request aborted."?
Это происходит, если вы отправили форму методом POST, но забыли добавить тег `{% csrf_token %}` внутрь тега `<form>` в HTML-шаблоне. Django блокирует такие запросы для предотвращения атак типа Cross-Site Request Forgery (межсайтовая подделка запроса).

### 7. Что такое CSRF-атака простыми словами?
Представьте, что вы авторизованы в своем онлайн-банке. Вы переходите по вредоносной ссылке на другой сайт. Тот сайт содержит скрытую форму, которая отправляет POST-запрос на перевод денег на сайт вашего банка. Браузер автоматически прикрепит к запросу ваши куки авторизации, и банк выполнит перевод. Защитный токен CSRF предотвращает это: вредоносный сайт не знает секретный токен, сгенерированный нашей системой, поэтому его запрос будет отклонен сервером банка.

### 8. Почему кастомный метод валидации поля должен называться именно `clean_<fieldname>()`?
Это соглашение об именовании в Django (Naming Convention). Под капотом метод `is_valid()` выполняет интроспекцию класса формы и ищет все методы, имена которых начинаются с префикса `clean_`. Он автоматически вызывает их для соответствующих полей во время общего цикла валидации.

### 9. В какой последовательности запускаются проверки при вызове `is_valid()`?
1. Сначала запускаются стандартные валидаторы самих полей (проверка длины, типов, обязательности).
2. Затем запускаются кастомные методы валидации отдельных полей `clean_<fieldname>()` (если они объявлены).
3. В последнюю очередь вызывается метод валидации всей формы `clean()`.

### 10. Как добавить CSS-класс к полю формы прямо в Python-коде?
Для этого нужно передать словарь с атрибутами в виджет поля через аргумент `attrs`. Например:
```python
name = forms.CharField(widget=forms.TextInput(attrs={'class': 'custom-input-class', 'id': 'my-unique-id'}))
```

### 11. Можно ли в форме использовать метод GET вместо POST?
Да, можно. Например, формы поиска обычно отправляются методом GET. В этом случае параметры запроса подставляются прямо в адресную строку (URL) вроде `/search/?query=django&category=books`. GET-формы не требуют CSRF-токена, так как они не изменяют состояние сервера и предназначены только для безопасного чтения данных.

### 12. Что будет, если метод `clean_fieldname()` не вернет значение?
Поле формы будет очищено в значение `None`. Это частая ошибка. Вы написали валидацию, проверили данные, бросили ошибку при несовпадении, но забыли написать `return value` в конце метода. В результате в `cleaned_data` запишется `None` вместо проверенного корректного значения.

### 13. Как вывести в шаблоне все ошибки формы в одном месте списком?
Для этого в шаблоне можно вывести `{{ form.errors }}`. Django сгенерирует ненумерованный список `<ul>` со всеми ошибками всех полей формы.

### 14. Как сделать так, чтобы поле формы не очищало пробелы по краям?
По умолчанию `CharField` и его подклассы используют параметр `strip=True`, который автоматически удаляет начальные и конечные пробелы (например, строка `"  Иван  "` превратится в `"Иван"`). Чтобы отключить это поведение, укажите `strip=False` при объявлении поля: `name = forms.CharField(strip=False)`.

### 15. Какая разница между ошибкой в `clean_<field>` и ошибкой в `clean`?
Ошибка в `clean_<field>` привязывается конкретно к данному полю. В шаблоне её можно отобразить через `{{ form.field.errors }}`. Ошибка в `clean` считается ошибкой всей формы на макроуровне (например, "данная комбинация дат недоступна"). В шаблоне она выводится через `{{ form.non_field_errors }}`.

---

## 10. Методические рекомендации преподавателю

### Сложные концепции и их объяснение

1.  **Жизненный цикл формы: Связанная (Bound) vs Несвязанная (Unbound).**
    Студентам часто трудно понять, почему один и тот же класс формы создается в коде View дважды в разных ветках `if/else`.
    *   *Аналогия:* Представьте форму как **пластиковую форму для выпечки**.
        *   Когда она пустая — это `Unbound` форма. Мы просто показываем её пользователю (GET). В нее нельзя "тыкнуть" валидацией, там нет теста.
        *   Когда мы заливаем в нее тесто (данные из `request.POST`) — она становится `Bound` (связанной). Теперь мы можем отправить её в духовку проверки (`is_valid()`). Если тесто вытекло (ошибки) — мы возвращаем форму с этим же тестом пользователю на доработку.
2.  **Забытый `return` в `clean_<fieldname>()`.**
    Это классическая методическая ловушка. Студенты пишут проверку, радуются, что она работает, но при отправке корректных данных получают `None` в базе данных или ошибку валидации далее по коду.
    *   *Объяснение:* Объясните ребятам, что метод `clean_*` называется так не просто так. Его задача — **очистить и вернуть** данные. Если мы не возвращаем значение, Python автоматически возвращает `None`. Преподавателю на доске рекомендуется написать схему:
        `Ввод (сырые данные) -> clean_field() -> Вывод (очищенные данные в cleaned_data)`. Если на выходе пустота (`None`), Django считает, что поле пустое.

### Стратегии дифференциации обучения

*   **Для сильных учеников (физ-мат профиль):**
    *   Предложите им реализовать динамические виджеты. Например, поле выбора предмета олимпиады должно динамически изменять свой список `choices` в зависимости от возраста, используя AJAX-запрос (или просто написать сложную валидацию на бэкенде).
    *   Попросите их написать собственный класс валидатора (callable-объект) вместо написания методов `clean_`. Это научит их принципу DRY (Don't Repeat Yourself).
    *   Предложите сделать кастомный рендеринг формы с помощью разметки Tailwind CSS или Bootstrap 5, где CSS-классы ошибок (`is-invalid`) навешиваются динамически на теги `input` при наличии ошибок в `form.errors`.
*   **Для слабых учеников:**
    *   Разрешите им на первых этапах использовать исключительно `{{ form.as_p }}` в шаблонах. Это избавит их от необходимости верстать сложные HTML-структуры вручную и позволит сосредоточиться на Python-коде во `views.py` и `forms.py`.
    *   Фокусируйтесь на базовой валидации (типы полей, `required=True/False`) до перехода к взаимной валидации в методе `clean()`.
    *   Подготовьте для них "каркас" (boilerplate) файла `views.py` с уже прописанным условием `if request.method == 'POST':`, чтобы они заполняли только логику работы с формой.
